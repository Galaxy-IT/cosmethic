nav.nav
  ul.nav__list
    each link, title in nav.navPages
      li.nav__item
        if (title == "Home")
          a(href= link).nav__link.active= title
        else
          a(href= link).nav__link= title